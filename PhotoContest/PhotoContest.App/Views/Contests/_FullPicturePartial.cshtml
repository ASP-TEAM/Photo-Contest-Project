@model PhotoContest.App.Models.ViewModels.Picture.FullPictureViewModel

<div class="picture">
    <img src="@Model.Url" class="img-thumbnail" />
    <p><strong>Uploader: </strong> @Model.UserName</p>
    <span class="badge">Votes @Model.Votes</span>
</div>